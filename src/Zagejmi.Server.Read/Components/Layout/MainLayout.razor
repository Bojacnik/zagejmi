@inherits LayoutComponentBase

<div class="min-h-screen bg-gradient-to-r from-primary-950 to-primary-900 text-text-default">
    <nav class="sticky top-0 h-16 w-full z-50 bg-primary-950/80 backdrop-blur-sm">
        <NavMenu />
    </nav>

    <div class="grid grid-cols-[8rem,1fr]">
        <!-- Sidebar -->
        <aside class="sticky top-16 h-[calc(100vh-4rem)] p-4">
            <div class="flex flex-col gap-2">
                <a class="text-center">control template 1</a>
                <a class="text-center">control template 2</a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="p-8">
            @Body
        </main>
    </div>
</div>