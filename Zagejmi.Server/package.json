{
  "dependencies": {
  },
  "devDependencies": {
    "tailwindcss": "^4.0.6",
    "@tailwindcss/cli": "^4.0.6"
  },
  "scripts": {
    "css:build": "npx @tailwindcss/cli -i app.css -o wwwroot/styles.css --watch",
    "css:releasebuild": "npx @tailwindcss/cli -i app.css -o wwwroot/styles.css --minify"
  }
}
